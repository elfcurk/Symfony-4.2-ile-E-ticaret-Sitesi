<form method="post" action="{{ path('shopcart_delete', {'id': shopcart.id}) }}" onsubmit="return confirm('Silmek istediğinize emin misiniz?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shopcart.id) }}">
    {{ include('messages.html.twig') }}
    <button class="btn">Sil</button>
</form>
